setup:
    composer:
        action: install

test:
    php_mess_detector:
        allow_failures: true
    php_spec: ~
    php_mess_detector:
        allow_failures: true
    php_code_sniffer:
        path: src
        standard: EnableIT
        allowed_errors: -1
        allowed_warnings: -1
